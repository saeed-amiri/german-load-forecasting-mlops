# configs/config.yaml

paths: {{ paths }}
  
# Parameters for SQL handling
sql: {{ sql }}

# logging settings for pipeline services
logging: {{ logging }}

# API settings
api: {{ api }}

# Auth settings
auth: {{ auth }}

# Training
train: {{ training }}
